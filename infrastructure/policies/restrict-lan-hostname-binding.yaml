apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: restrict-lan-hostname
  labels:
    app.kubernetes.io/part-of: policies
spec:
  policyName: restrict-lan-hostname
  validationActions: [Deny, Audit]
