apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels:
    # node-exporter requires hostNetwork/hostPID/hostPath — same pattern as longhorn-system
    pod-security.kubernetes.io/enforce: privileged
