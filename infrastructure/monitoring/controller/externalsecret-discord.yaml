apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: monitoring-discord-int-grafana
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: monitoring-discord-int-grafana
  dataFrom:
    - extract:
        key: monitoring-discord-int-grafana
