apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: monitoring-healthchecks
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: monitoring-healthchecks
  dataFrom:
    - extract:
        key: monitoring-healthchecks
